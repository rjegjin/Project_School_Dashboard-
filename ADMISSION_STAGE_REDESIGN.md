# 고입 희망-접수-결과 단계 분리 설계

작성일: 2026-06-09

## 진행 기록

### 2026-06-09

- 1단계 완료: `V:X`를 실제 접수 블록으로 정의했다.
- 2단계 완료: 로컬 CSV 백업을 생성했다.
  - 백업 위치: `/home/rjegj/projects/Project_HighSchool_apply_Dashboard/backups/admission_stage_20260609_205508`
  - 백업 대상: `📌 메인`, `입시_트래킹`, `입시 진행 현황`, `301~314`
  - Google Drive 전체 사본 생성은 Drive API `userRateLimitExceeded`로 실패했다.
- 3단계 완료: `301~314` 전체 시트의 `V1:X1` 헤더를 아래 값으로 변경했다.
  - `V1`: `실제접수_영재고`
  - `W1`: `실제접수_과학고`
  - `X1`: `실제접수_자사고`
- 검증 완료: `301~314` 전체 `V1:X1` 값이 새 헤더와 일치한다.

## 목적

현재 고입 자동화는 반별 시트의 희망 입력을 `입시_트래킹`의 최종 지원값처럼 승격시키는 문제가 있다.
이 문서는 학생의 희망, 실제 접수, 전형 결과를 분리해 관리하기 위한 기준 문서다.

## 핵심 원칙

- `H:P`는 희망 단계다.
- `V:X`는 실제 접수 단계다.
- `Y:AB`는 전형 진행 및 결과 단계다.
- 희망값은 자동으로 최종값이 되면 안 된다.
- 실제 접수 학생 수는 희망 학생 수와 별도 지표로 집계한다.

## 반별 시트 역할

### `H:P` 희망 입력 블록

학생 또는 담임이 입력한 희망 유형과 희망 학교를 나타낸다.

| 열 | 의미 |
| --- | --- |
| H | 영재고 희망 |
| I | 과학고 희망 |
| J | 예술계고 희망 |
| K | 마이스터고/특성화고 희망 |
| L | 특성화/마이스터 학과 |
| M | 자사고 희망 |
| N | 국제고/외국어고 희망 |
| O | 일반고 희망 |
| P | 기타/대안 희망 |

입력 규칙:

- 빈칸: 해당 유형 희망 없음
- `O`, `○`, `V`, `1`, `예`: 해당 유형 희망 있음, 학교명 미정
- 학교명: 해당 유형 희망 있음, 희망 학교명 입력
- `X`: 명시적 비희망 또는 취소

### `V:X` 실제 접수 블록

담임 또는 관리자가 확인한 실제 접수 여부를 나타낸다.
1단계 기준으로 이 블록의 의미를 아래처럼 고정한다.

| 열 | 새 의미 | 권장 헤더 |
| --- | --- | --- |
| V | 영재고 실제 접수 | `실제접수_영재고` |
| W | 과학고 실제 접수 | `실제접수_과학고` |
| X | 자사고 실제 접수 | `실제접수_자사고` |

입력 규칙:

- 빈칸: 실제 접수 확인 안 됨
- `O`, `○`, `V`, `1`, `예`: 해당 유형 실제 접수 확인, 접수 학교는 희망학교 fallback
- 학교명: 해당 유형 실제 접수 확인, 접수 학교는 입력된 학교명
- `X`: 접수 안 함 또는 철회 확인

복수 입력 규칙:

- `V:X` 중 둘 이상 접수 표시가 있으면 `복수접수 확인필요`로 처리한다.
- 1차 구현에서는 대표 접수유형을 `영재고 -> 과학고 -> 자사고` 우선순위로 선택한다.
- 복수 접수 이력 자체는 대시보드 또는 확인 로그에 남겨야 한다.

### `Y:AB` 전형 진행/결과 블록

전형 단계별 진행 결과를 나타낸다.

| 열 | 의미 |
| --- | --- |
| Y | 1차 |
| Z | 2차 |
| AA | 3차 |
| AB | 최종 |

입력 규칙은 결과 수집 방식이 확정된 뒤 별도 정리한다.
현재는 희망/접수 분리 이후 결과 단계에서만 `최종유형`, `최종학교`를 확정한다.

## `입시_트래킹` 역할

| 컬럼 | 원천 | 갱신 정책 |
| --- | --- | --- |
| 희망유형 | 반별 시트 `H:P` | 자동 갱신 |
| 희망학교 | 반별 시트 `H:P` | 자동 갱신 |
| 접수유형 | 반별 시트 `V:X` | 자동 갱신 |
| 접수학교 | 반별 시트 `V:X`, 필요 시 희망학교 fallback | 자동 갱신 |
| 접수상태 | 반별 시트 `V:X` 판정 | 자동 갱신 |
| 최종유형 | 결과 입력 또는 관리자 확정 | 희망값으로 자동 채우지 않음 |
| 최종학교 | 결과 입력 또는 관리자 확정 | 희망값으로 자동 채우지 않음 |
| 데이터상태 | 희망/접수/결과 판정 | 자동 갱신 |

기존 `유형`, `지원학교`는 legacy 호환용으로 유지하되, 새 보고서의 기준 컬럼으로 쓰지 않는다.

## 데이터상태 정의

| 상태 | 의미 |
| --- | --- |
| `미입력` | 희망도 접수도 없음 |
| `희망만` | 희망은 있으나 실제 접수 확인 없음 |
| `접수확정` | 실제 접수 확인 있음 |
| `희망/접수 불일치` | 희망유형과 접수유형이 다름 |
| `복수접수 확인필요` | 실제 접수 블록에 둘 이상 접수 표시 |
| `결과입력` | 1차, 2차, 3차, 최종 중 결과값 있음 |
| `최종완료` | 최종 결과가 합격/불합격 등으로 정리됨 |

## 자동화 변경 방향

1. `sync_type_to_tracking.py`에 `V:X` 실제 접수 파서를 추가한다.
2. `최종유형`, `최종학교`를 희망값으로 자동 채우는 로직을 제거한다.
3. `희망유형/희망학교`는 `H:P`에서만 갱신한다.
4. `접수유형/접수학교/접수상태`는 `V:X`에서만 갱신한다.
5. dry-run 출력에서 희망, 접수, 데이터상태 변경 후보를 분리해 보여준다.
6. 대시보드는 `희망 현황`, `실제 접수 현황`, `결과 현황`을 분리한다.

## 단계별 작업 순서

### 1단계: `V:X` 의미 고정

- `V`는 영재고 실제 접수로 정의한다.
- `W`는 과학고 실제 접수로 정의한다.
- `X`는 자사고 실제 접수로 정의한다.
- 입력값의 의미를 빈칸, 긍정 표시, 학교명, `X`로 구분한다.
- 이 단계에서는 실제 Google Sheet 헤더와 데이터는 아직 변경하지 않는다.

### 2단계: 백업

- Google Sheet 전체 사본을 만든다.
- 최소한 `301~314`, `입시_트래킹`, `입시 진행 현황`을 export한다.
- 기존 `최종유형`, `최종학교`, `데이터상태` 값을 보존한다.

### 3단계: 반별 시트 헤더 변경

- `301~314` 전체 시트의 `V:X` 헤더를 `실제접수_영재고`, `실제접수_과학고`, `실제접수_자사고`로 변경한다.
- 열 위치는 바꾸지 않는다.

### 4단계: 접수 파서 구현

- `sync_type_to_tracking.py`에 `RECEIPT_TYPE_MAP = {21: "영재고", 22: "과학고", 23: "자사고"}`를 추가한다.
- `V:X` 값을 읽어 `접수유형`, `접수학교`, `접수상태`를 계산한다.
- 복수 접수는 대표 접수유형과 확인필요 상태를 함께 남긴다.

### 5단계: 최종 자동 승격 제거

- 희망유형 또는 legacy 유형이 `최종유형`으로 자동 복사되지 않게 한다.
- `최종유형`, `최종학교`는 결과 입력 또는 관리자 확정 때만 갱신한다.

### 6단계: 301 단일 반 검증

- `301` 탭에만 테스트 입력을 넣는다.
- `sync_type_to_tracking.py 2026 --dry-run --apply-schema --no-legacy-fill`로 확인한다.
- `입시_트래킹` 변경 후보가 의도와 맞는지 검토한다.

### 7단계: 전체 반 적용

- `301~314` 전체에 동일 기준을 적용한다.
- 전체 dry-run 후 실제 sync를 실행한다.

### 8단계: 대시보드 분리

- 희망 기준 보고서와 접수 기준 보고서를 분리한다.
- 기존 전체 현황은 기준 컬럼을 제목과 화면에 명시한다.

## 현재 주의점

- 현재 `입시_트래킹`에는 `접수유형`이 모두 비어 있는데도 `데이터상태=최종확정`인 행이 많다.
- 원인은 희망값 또는 legacy 유형이 `최종유형`으로 자동 승격된 구조다.
- 따라서 기존 `최종확정` 값은 실제 최종 확정으로 신뢰하면 안 된다.
