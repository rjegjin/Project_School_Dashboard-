# 🔗 통계 분석 프로그램 통합 가이드

---

## 📋 개요

**Project_HighSchool_apply_Analytics** (심층 통계 분석 도구)가 통합 대시보드와 어떻게 연계되는지를 설명합니다.

> 대시보드는 **실시간 추적**, 분석 프로그램은 **사후 분석** 담당

---

## 🎯 두 시스템의 역할 분담

### 📊 통합 대시보드 (Streamlit)

```
🎯 목적:  실시간 진학 현황 추적 및 관리
⏱️  시기:  지원 접수 ~ 최종 배정 (진행 중)
📁 데이터: Google Sheets (반별 시트, 입시_트래킹)
🛠️  기능: 실시간 입력 모니터링, 동기화, 검증
📤 출력: 웹 대시보드, 데이터 필터링, 통계
👥 대상: 담임, 관리자, 진학지도 담당자
```

**주요 탭:**
- 📈 전체 현황 — 지원 통계
- 🔍 학생 조회 — 학생별 상세 정보
- 📋 데이터 검증 — 입력 완성도, 오류 감지
- 📈 필터링 분석 — 조건부 현황 확인
- 🎓 학교 분석 — 배정학교 현황
- 🔧 관리 도구 — 동기화, 보고서 생성

---

### 🔬 통계 분석 프로그램 (Python)

```
🎯 목적:  결과 분석 및 심층 통계 연구
⏱️  시기:  최종 배정 완료 후 (분석 단계)
📁 데이터: Excel 파일 (처리된 배정 결과)
🛠️  기능: 고급 통계, 군집 분석, 네트워크 분석
📤 출력: HTML 리포트, Excel 분석표, PNG 시각화
👥 대상: 분석가, 학사관리자, 정책 입안자
```

**분석 모듈:**
- 🔍 PCA/Factor Analysis — 다변량 차원 축소
- 🎭 GMM Clustering — 학교 유형화 (선호/기피 패턴)
- 📊 Shannon Entropy — 지망/배정 다양성 지수
- 🌐 Network Centrality — 지역-학교 관계 네트워크
- 🗺️  Spatial Analysis — 지역별/학교별 흐름 분석

---

## 📊 데이터 흐름

```
┌────────────────────────────────────────────────────┐
│  Google Sheets (2026 실시간)                       │
│  ├─ 반별 시트 (담임 입력) → 지원 현황             │
│  └─ 입시_트래킹 (마스터) → 유형/1차/2차/최종/배정 │
└────────────┬─────────────────────────────────────┘
             │
             ├─→ [대시보드]
             │   실시간 추적, 검증, 필터링
             │
             └─→ [최종 배정 완료] ▼

┌────────────────────────────────────────────────────┐
│  Excel 파일 (배정 결과) + CSV (지역정보)          │
│  ├─ 2026학년도 후기고 최종결과.xlsx               │
│  ├─ 학생 상세정보 (이름, 반, 배정학교)            │
│  └─ 지역 코드 (행정동)                            │
└────────────┬─────────────────────────────────────┘
             │
             ├─→ [데이터 전처리]
             │   • PII 비식별화 (익명화)
             │   • 지역 정보 병합
             │   • 배정 결과 분류
             │
             └─→ [분석 실행] ▼

┌────────────────────────────────────────────────────┐
│  분석 결과                                          │
│  ├─ Step1: 전처리 및 익명화                        │
│  ├─ Step2: 지망선호도 및 지역흐름                  │
│  ├─ Step3: 학교유형화 및 통계검증                  │
│  ├─ Step4: 대학원수준 심층분석                     │
│  └─ Output: HTML 대시보드 + PNG 차트               │
└────────────┬─────────────────────────────────────┘
             │
             └─→ [대시보드에 통합] ← 다음 단계
                  옵션 A/B 선택
```

---

## 🔗 통합 방안

### ✨ 옵션 A: 경량 통합 (권장 - 단기)

> **특징:** 분석 결과 HTML을 대시보드의 新탭에서 뷰어로 표시

**장점:**
- ⚡ 구현 간단 (HTML embed)
- 🔄 실행 독립성 유지
- 🛠️  관리 용이
- 📈 추후 확장 가능

**구현 방식:**
```python
# 대시보드에 새 탭 추가
with tab_analytics:
    st.header("📊 심층 분석 결과")

    # 생성된 HTML 파일들 표시
    output_dir = "../Project_HighSchool_apply_Analytics/output/"
    html_files = [f for f in os.listdir(output_dir)
                  if f.endswith('.html')]

    selected = st.selectbox("분석 보고서", html_files)

    if selected:
        with open(os.path.join(output_dir, selected), 'r') as f:
            html_content = f.read()
        st.components.v1.html(html_content, height=800)
```

**작동 흐름:**
```
1. 분석 프로그램이 독립 실행
   ↓
2. Excel과 HTML 생성 (output/ 디렉토리)
   ↓
3. 대시보드가 완성된 HTML 파일을 읽음
   ↓
4. 사용자가 대시보드에서 시각화 조회
```

**추천 시기:** **PHASE 4 (9월~10월)**

---

### 🚀 옵션 B: 데이터 연동 (중기)

> **특징:** 분석 Excel의 처리 데이터를 대시보드에서 직접 로드/시각화

**장점:**
- 🎯 더 동적인 인터랙션
- 🔍 필터링/선택 가능
- ⚡ 실시간 재계산 불필요
- 📊 통합된 사용자 경험

**구현 방식:**
```python
@st.cache_data(ttl=3600)
def load_analytics_results():
    """분석 결과 Excel 로드"""
    base = "../Project_HighSchool_apply_Analytics/data/processed"

    dfs = {}
    sheets = ['1_유형화(신뢰데이터)', '1_군집요약', '2_종속성검정_결과']

    for sheet in sheets:
        try:
            df = pd.read_excel(
                f"{base}/Step3_학교유형화_및_통계검증.xlsx",
                sheet_name=sheet
            )
            dfs[sheet] = df
        except:
            pass

    return dfs

# 대시보드 탭에서 사용
with tab_analytics:
    st.header("📊 학교 유형화 분석")
    results = load_analytics_results()

    if '1_유형화(신뢰데이터)' in results:
        df_cluster = results['1_유형화(신뢰데이터)']

        # 필터링 UI
        cluster_type = st.multiselect("학교 유형",
                                      df_cluster['유형'].unique())
        if cluster_type:
            df_filtered = df_cluster[df_cluster['유형'].isin(cluster_type)]
            st.dataframe(df_filtered, use_container_width=True)
```

**작동 흐름:**
```
1. 분석 프로그램이 Step3 Excel 생성
   ↓
2. 대시보드가 주기적으로(3600초) 새 데이터 로드
   ↓
3. 사용자가 대시보드에서 필터링/탐색
   ↓
4. 동적 시각화 및 인터랙션
```

**추천 시기:** **PHASE 5 (11월~12월)**

---

### 🎯 옵션 C: 완전 통합 (장기)

> **특징:** 분석 로직을 대시보드에 내장, 실시간 재계산

**장점:**
- 🌟 완전한 통합 경험
- ⚙️  실시간 분석 가능

**단점:**
- 🔴 매우 복잡
- 💻 계산 비용 높음
- 🛠️  유지보수 어려움

**평가:** 현재로서는 **권장하지 않음**

---

## 📈 추천 통합 로드맵

### PHASE 4 (9월~10월): 옵션 A 구현

```
📅 일정          🎯 작업                     📊 결과
────────────────────────────────────────────────────
9월 초     분석 프로그램 데이터 준비
           ├─ 2025 최종결과 (2026 참고)
           └─ 지역 정보 병합

9월 중     분석 실행
           ├─ Step 1-4 완료
           └─ Output HTML/Excel 생성

9월 말     옵션 A 구현
           ├─ 대시보드에 "📊 심층분석" 탭 추가
           ├─ HTML 뷰어 통합
           ├─ selectbox로 보고서 선택 기능
           └─ 테스트 & 배포

10월 초    유지보수
           └─ 사용자 피드백 반영
```

### PHASE 5 (11월~12월): 옵션 B 개선

```
📅 일정          🎯 작업                     📊 결과
────────────────────────────────────────────────────
11월 중    옵션 B 프로토타입
           ├─ 분석 결과 데이터 로드
           ├─ 기본 필터링/시각화
           └─ 반응성 테스트

12월      배포 & 피드백
           └─ 사용자 경험 개선
```

---

## ✅ 데이터 준비 체크리스트

### 입력 데이터 준비

```
□ 2026 최종 배정 결과 파일
  └─ 경로: /home/rjegj/다운로드/2026학년도 후기고 최종결과.xlsx

□ 학생 지역정보 (행정동 또는 주소)
  └─ 추천: 학적 시스템 또는 기존 명렬표

□ 학교 정보
  └─ 학교명 → 좌표/지역 매핑 (사전 구성)
```

### 프로그램 환경

```
□ Python 패키지 설치
  ├─ pandas, openpyxl      (데이터)
  ├─ scikit-learn          (PCA, GMM)
  ├─ scipy, networkx       (통계/네트워크)
  ├─ plotly, matplotlib    (시각화)
  └─ statsmodels           (고급 통계)

□ 분석 프로그램 경로
  └─ /home/rjegj/projects/Project_HighSchool_apply_Analytics/

□ 출력 경로 확인
  └─ .../output/ 디렉토리 존재 및 쓰기 권한
```

---

## 📊 분석 결과 해석 가이드

### 1️⃣ 학교 유형화 (Clustering)

```
유형 A: 선호도 높음  ⭐⭐⭐⭐⭐
  • 경쟁률 높음, 만족도 높음
  • 대응: 현황 유지, 수용능력 평가

유형 B: 중상  ⭐⭐⭐⭐
  • 평균 수준
  • 대응: 정상 배정

유형 C: 기피 경향  ⭐⭐
  • 경쟁률 낮음, 만족도 낮음
  • 원인: 교육여건? 위치? 평판?
  • 대응: 인프라 개선 또는 홍보 강화

유형 D: 미달  ⭐
  • 지원자 부족
  • 대응: 모집 전략 재검토
```

### 2️⃣ 엔트로피 지수 (다양성)

```
높음 (5 이상)  📈
  → 지망/배정이 다양함
  → 자유도 높음, 선택권 다양

중간 (3~5)  →
  → 일정한 선호 있음
  → 자연스러운 흐름

낮음 (0~3)  📉
  → 쏠림 현상 발생
  → 특정 학교로의 집중 또는 강제 배정
```

### 3️⃣ 네트워크 중심성

```
높은 중심성  🌟
  → 영향력 있는 지역/학교
  → 지역-학교 간 강한 유대
  → 변경 시 반발 가능성 높음

낮은 중심성  ○
  → 주변부 지역/학교
  → 다른 선택지 많음
  → 유연성 있음
```

---

## 🚀 다음 단계

### ✅ 즉시 (3월)
- [x] ANNUAL_WORKFLOW.md 작성
- [x] 이 통합 가이드 문서 작성
- [x] 대시보드 12탭 구성

### 📋 단기 (4월~8월)
- [ ] 2026 데이터 적립 진행
- [ ] 분석 프로그램 입력 데이터 준비

### 🚀 중기 (9월~10월)
- [ ] **옵션 A 구현** ← 여기서 시작!
  1. 분석 프로그램 실행
  2. 대시보드 새 탭 추가
  3. HTML 임베딩
  4. 사용자 테스트

### 🎯 장기 (11월~12월)
- [ ] **옵션 B 개선**
- [ ] 데이터 연동 시작
- [ ] 반응성 최적화

---

## 🔧 문제 해결

### ❓ HTML 파일이 보이지 않음

**확인 사항:**
```bash
# 경로와 파일 확인
ls -la ../Project_HighSchool_apply_Analytics/output/

# 파일 목록 출력
python -c "
import os
path = '../Project_HighSchool_apply_Analytics/output/'
print([f for f in os.listdir(path) if f.endswith('.html')])
"
```

### ❓ 분석 프로그램이 실패함

**확인 사항:**
```
□ Step2 Excel 파일 존재?
□ 필수 컬럼 존재? (학교, 경쟁률, 만족도 등)
□ 인코딩 문제? (utf-8-sig 권장)
□ 지역 매핑 데이터 준비?
```

### ❓ 대시보드에 한글이 깨짐

**확인 사항:**
```html
<!-- HTML 파일의 charset 확인 -->
<meta charset="utf-8">

<!-- 또는 -->
<meta charset="utf-8-sig">
```

---

## 📞 참고 자료

| 파일 | 용도 |
|:---:|:---:|
| `integrated_dashboard.py` | 메인 대시보드 |
| `ANNUAL_WORKFLOW.md` | 연간 일정 |
| `Project_HighSchool_apply_Analytics/` | 분석 도구 |
| `/tmp/analysis_logs/` | 분석 실행 로그 |

---

## 💡 설계 철학

> **"분석의 복잡성은 숨기고, 결과의 명확성은 드러낸다"**

- 분석 프로그램과 대시보드는 **독립적으로 작동**
- 사용자는 **대시보드에서만 상호작용**
- 결과 생성은 **백그라운드에서 자동화**

---

**최종 수정:** 2026-03-22
**버전:** v1.1 (설계 개선)
